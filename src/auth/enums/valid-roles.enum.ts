/**
 * Application roles. Stored on the User as a string array (text[]) following the
 * course convention, so a user may hold one or more roles.
 */
export enum ValidRoles {
  superadmin = 'superadmin',
  user = 'user',
}
